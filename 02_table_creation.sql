CREATE TABLE orders (
    order_id INT,
    order_date DATE,
    product_id VARCHAR(10),
    defect_type VARCHAR(20),
    warehouse VARCHAR(5),
    quantity INT,
    status VARCHAR(15),
    employee_id VARCHAR(10)
);
